import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFFD4A574);
  static const Color primaryLight = Color(0xFFF2C078);

  // Background & Surface
  static const Color background = Color(0xFF0F0F10);
  static const Color surface = Color(0xFF1A1A1C);
  static const Color surfaceElevated = Color(0xFF232326);

  // Borders
  static const Color border = Color(0xFF2E2E32);

  // Semantic
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE57373);
  static const Color warning = Color(0xFFF6C453);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFC9C9CC);
  static const Color textMuted = Color(0xFF8E8E93);
}